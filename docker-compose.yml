version: "3.9"
services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      APP_ADDR: ":8080"
      DB_PATH: "file:metrics.db?_pragma=foreign_keys(1)"
      JWT_SECRET: "compose-dev-secret"

